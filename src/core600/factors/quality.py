"""Quality factor placeholders."""

import pandas as pd


def calculate_quality_factors(frame: pd.DataFrame) -> pd.DataFrame:
    """Return quality-factor input unchanged until real formulas are defined."""

    return frame.copy()
