"""Growth factor placeholders."""

import pandas as pd


def calculate_growth_factors(frame: pd.DataFrame) -> pd.DataFrame:
    """Return growth-factor input unchanged until real formulas are defined."""

    return frame.copy()
