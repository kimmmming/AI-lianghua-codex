"""Momentum factor placeholders."""

import pandas as pd


def calculate_momentum_factors(frame: pd.DataFrame) -> pd.DataFrame:
    """Return momentum-factor input unchanged until real formulas are defined."""

    return frame.copy()
