"""Risk scoring placeholders."""

import pandas as pd


def calculate_risk_deductions(frame: pd.DataFrame) -> pd.DataFrame:
    """Return risk input unchanged until real formulas are defined."""

    return frame.copy()
